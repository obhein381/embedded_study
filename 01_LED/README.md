# LED

## Objective

Learn how to control an LED using an ESP32.

## What I Learned

- `setup()` runs once.
- `loop()` runs continuously.
- `pinMode()` configures a GPIO pin as an output.
- `digitalWrite()` changes the output state.
- Without `delay()`, the LED toggles too quickly for the human eye to perceive.